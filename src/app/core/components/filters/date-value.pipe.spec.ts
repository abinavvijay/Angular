import { DateValuePipe } from './date-value.pipe';

describe('DateValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DateValuePipe();
    expect(pipe).toBeTruthy();
  });
});
