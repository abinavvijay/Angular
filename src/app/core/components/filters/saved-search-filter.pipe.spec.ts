import { SavedSearchFilterPipe } from './saved-search-filter.pipe';

describe('SavedSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SavedSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
