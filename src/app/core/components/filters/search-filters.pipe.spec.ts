import { SearchFiltersPipe } from './search-filters.pipe';

describe('SearchFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
